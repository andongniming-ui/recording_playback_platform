[global]
index-url = http://pypi.intranet.example.com/simple
trusted-host = pypi.intranet.example.com
timeout = 120
retries = 5

# Optional fallback mirror inside the intranet.
# extra-index-url = http://pypi-mirror.intranet.example.com/simple
