server:
  port: 29081

spring:
  datasource:
    url: jdbc:mysql://${CREDIT_DB_HOST:127.0.0.1}:${CREDIT_DB_PORT:3307}/${CREDIT_DB_NAME:credit_demo}?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: ${CREDIT_DB_USER:root}
    password: ${CREDIT_DB_PASSWORD:root123}
  sql:
    init:
      mode: always

credit:
  mock:
    base-url: ${CREDIT_MOCK_BASE_URL:http://127.0.0.1:29083}
  internal:
    base-url: ${CREDIT_INTERNAL_BASE_URL:http://127.0.0.1:29081}
  features:
    enable-external-cache: true
    enable-contact-stability: true
